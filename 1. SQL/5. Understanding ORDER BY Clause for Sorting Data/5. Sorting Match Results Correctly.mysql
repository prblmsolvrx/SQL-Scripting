"""
identify all Lionel Messi's matches played after the year 2005.
You will need to modify the provided SQL query by adding an appropriate WHERE clause.
"""

SELECT match_id, date
FROM Matches 
WHERE YEAR(date) > 2005
ORDER BY date DESC;